#!/bin/bash

#This scripts generates a list of random passwords.

# A random number as a password

PASSWORD="${RANDOM}" #Variable Password is assigned to RANDOM which is an in-built in bash funciton for generating random numbers.
echo "${PASSWORD}"